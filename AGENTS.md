# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v57.0.0/ before writing any code.

# Git workflow

`main` is the default branch. Keep it clean:

- Merge feature/work branches into `main` as soon as their work is done -
  don't leave them open or let them linger.
- Delete a branch immediately after it's merged into `main`. No stale
  branches hanging around.
